import{useEffect,useRef}from'react';import type{Station}from'../types';
type S=Station&{pm?:number};
export function AirMap({stations}:{stations:S[]}){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const c=ref.current;if(!c)return;const dpr=devicePixelRatio||1,box=c.getBoundingClientRect();c.width=box.width*dpr;c.height=box.height*dpr;const x=c.getContext('2d')!;x.scale(dpr,dpr);x.fillStyle='#e9edc9';x.fillRect(0,0,box.width,box.height);x.strokeStyle='rgba(110,101,85,.14)';x.lineWidth=10;for(let i=-box.height;i<box.width;i+=58){x.beginPath();x.moveTo(i,0);x.lineTo(i+box.height,box.height);x.stroke()}stations.forEach(s=>{const px=box.width*(.12+((s.lng-79.84)/.085)*.76),py=box.height*(.12+((6.999-s.lat)/.14)*.76);if(s.pm!==undefined){const r=22+Math.min(s.pm,60)*1.05,g=x.createRadialGradient(px,py,3,px,py,r);const col=s.pm<=25?'142,189,150':s.pm<=50?'216,220,130':'250,157,69';g.addColorStop(0,`rgba(${col},.58)`);g.addColorStop(1,`rgba(${col},0)`);x.fillStyle=g;x.beginPath();x.arc(px,py,r,0,Math.PI*2);x.fill()}x.fillStyle='#3a342b';x.beginPath();x.arc(px,py,3.5,0,Math.PI*2);x.fill();});},[stations]);return <canvas ref={ref} role="img" aria-label={`Atmospheric field from ${stations.length} stations`}/>}
