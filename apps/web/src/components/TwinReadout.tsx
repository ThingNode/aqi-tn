export function TwinReadout({sl,band,pm}:{sl?:number;band?:string;pm?:number}){const ready=sl!==undefined&&pm!==undefined&&band!==undefined,above=ready&&pm>15;return <section className="twin" aria-label={ready?`Sri Lanka AQI ${sl}, ${band}; PM2.5 ${pm} micrograms per cubic metre, ${above?'above':'within'} WHO guideline`:'Air-quality reading unavailable'}><div><div className="eyebrow">Sri Lanka AQI</div><div className="value numeric">{sl??'—'}</div><div className="headline">{band??'Awaiting reading'}</div><div className="datum">{ready?'Driven by PM2.5':'No sample data shown'}</div></div><div><div className="eyebrow">WHO guideline</div><div className="who-headline">{ready?(above?<>Above<br/>guideline</>:<>Within<br/>guideline</>):<>Awaiting<br/>reading</>}</div><div className="headline numeric">{ready?pm.toFixed(1):'—'} <small>µg/m³</small></div><div className="datum numeric">Guideline 15 µg/m³</div></div></section>}
